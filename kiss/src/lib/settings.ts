/**
 * @fileoverview Contains the spreadsheet IDs to both the
 *    config and the data. This is used to know which spreadsheet
 *    should the program fetch data from
 */
const settings = {
  spreadsheetID: 'spreadsheet_id_here',
  configID: 'config_spreadsheet_id_here',
  apiKey: 'api_key_here'
}

export default settings
